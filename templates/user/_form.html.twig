{{ form_start(form) }}
    {{ form_widget(form) }}
<div class="d-flex justify-content-between mt-5">
    <button class="btn custom-btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}
    {{ include('user/_delete_form.html.twig') }}
</div>
