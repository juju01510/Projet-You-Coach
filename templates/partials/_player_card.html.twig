<div class="card-club card-team-back d-flex box-shadow">
    <a class="link" href="{{ path('app_user_show', {'id': player.id}) }}"></a>
    <div class="card-title-team">
        <p>JOUEUR</p>
    </div>
{#    <div class="card-image col-4 d-flex justify-content-center align-items-center">#}
{#        <img src="{{ asset('uploads/images/' ~ team.photo) }}" alt="Logo du club">#}
{#    </div>#}
    <div class="card-content col-8">
        <h2 class="card-text">{{ player.firstname }} {{ player.lastname }}</h2>
                <p>Poste : XXX </p>
                <p>Pied fort : XXX</p>
                <p>Age : XXX</p>

        {% if is_granted('ROLE_PLAYER') %}
        {% else %}
        <p>
            <a class="z-1 position-absolute custom-btn-light"
               href="{{ path('app_team_edit', {'id': team.id}) }}">Modifier</a>
        </p>
        {% endif %}

    </div>
</div>