{{ form_start(form) }}
    {{ form_row(form.level) }}
    {{ form_row(form.photo) }}
    {{ form_row(form.category) }}
<div>
    <button class="custom-btn mt-3">{{ button_label|default('Ajouter') }}</button>
</div>
{{ form_end(form) }}


