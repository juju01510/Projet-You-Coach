{% extends 'base.html.twig' %}

{% block title %}Training{% endblock %}

{% block main %}

    <div class="container cool">
        <h1>Entrainement</h1>

        {% if is_granted('ROLE_PLAYER') %}
        {{ form_start(formPresense) }}
        {{ form_widget(formPresense) }}
        <button type="submit" class="custom-btn">Envoyer</button>
        {{ form_end(formPresense) }}
        {% endif %}

        <table class="table">
            <tbody>
            {#            <tr> #}
            {#                <th>Id</th> #}
            {#                <td>{{ training.id }}</td> #}
            {#            </tr> #}
            <tr>
                <th>Date</th>
                <td>{{ training.date ? training.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Place</th>
                <td>{{ training.place }}</td>
            </tr>
            <tr>
                <th>Infos</th>
                <td>{{ training.info }}</td>
            </tr>
            <tr>
                <th>Joueurs présents</th>
                <th></th>
            </tr>

            {% for player in players %}
                <tr>
                    <th></th>
                    <td>{{ player.firstname }} {{ player.lastname }}</td>
                </tr>
            {% endfor %}

            </tbody>
        </table>

        {#        <a class="dark-color" href="{{ path('app_training_index') }}">back to list</a> #}

        {#        <a class="dark-color" href="{{ path('app_training_edit', {'id': training.id}) }}">edit</a> #}

        {#        {{ include('training/_delete_form.html.twig') }} #}
    </div>

{% endblock %}
