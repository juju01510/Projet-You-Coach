{% extends 'base.html.twig' %}

{% block title %}Training{% endblock %}

{% block main %}

    <div class="cool">
        <h1>Entrainement</h1>

        <table class="table">
            <tbody>
            {#            <tr> #}
            {#                <th>Id</th> #}
            {#                <td>{{ training.id }}</td> #}
            {#            </tr> #}
            <tr>
                <th>Date</th>
                <td>{{ training.date ? training.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Place</th>
                <td>{{ training.place }}</td>
            </tr>
            <tr>
                <th>Info</th>
                <td>{{ training.info }}</td>
            </tr>
            <tr>
                <th>Joueurs</th>
            </tr>

            {% for player in training.team.player %}
                <tr>
                    <th></th>
                    <td>{{ player.firstname }} {{ player.lastname }}</td>
                </tr>
            {% endfor %}

            </tbody>
        </table>

        {#        <a class="dark-color" href="{{ path('app_training_index') }}">back to list</a> #}

        {#        <a class="dark-color" href="{{ path('app_training_edit', {'id': training.id}) }}">edit</a> #}

        {#        {{ include('training/_delete_form.html.twig') }} #}
    </div>

{% endblock %}
